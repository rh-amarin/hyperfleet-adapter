adapterConfig:
  create: true
  files:
    adapter-config.yaml: examples/maestro-two-resources/adapter-config.yaml

adapterTaskConfig:
  create: true
  files:
    task-config.yaml: examples/maestro-two-resources/adapter-task-config.yaml
    manifestwork-namespace.yaml: examples/maestro-two-resources/adapter-task-resource-manifestwork-namespace.yaml
    manifestwork-configmap.yaml: examples/maestro-two-resources/adapter-task-resource-manifestwork-configmap.yaml

broker:
  create: true
  googlepubsub:
    projectId: CHANGE_ME
    subscriptionId: CHANGE_ME
    topic: CHANGE_ME
    deadLetterTopic: CHANGE_ME

image:
  registry: CHANGE_ME
  repository: hyperfleet-adapter
  pullPolicy: Always
  tag: latest

env:
  - name: NAMESPACE
    valueFrom:
      fieldRef:
        fieldPath: metadata.namespace
  - name: SERVICE_ACCOUNT
    valueFrom:
      fieldRef:
        fieldPath: spec.serviceAccountName
  - name: SIMULATE_RESULT
    value: success     # other possible values: success, failure, hang, crash, invalid-json, missing-status

rbac:
   resources:
     - namespaces
     - serviceaccounts
     - configmaps
     - deployments
     - roles
     - rolebindings
     - jobs
     - jobs/status
     - pods
